language: java
jdk:
- oraclejdk8
sudo: false
cache:
  directories:
  - $HOME/.m2
before_install:
- ./installPaper.sh
install:
- mvn install -DfinalName=release -DskipTests=true -Dmaven.javadoc.skip=true -B -V
